import textField from 'empouch-attach/utils/text-field';
import { module, test } from 'qunit';

module('Unit | Utility | text field');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = textField();
  assert.ok(result);
});
